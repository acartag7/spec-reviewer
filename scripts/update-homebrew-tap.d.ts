export function assertTapFormulaVersionCanAdvance(existing: string, incoming: string): void;
export function compareReleaseVersions(left: string, right: string): -1 | 0 | 1;
export function isTapFormulaVersionOlder(existing: string, incoming: string): boolean;
